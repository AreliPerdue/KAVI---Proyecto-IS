/** Fase 8 (T031): implementaciones Supabase pendientes durante la etapa frontend-first. */
export function notImplemented(name: string): never {
  throw new Error(`Pendiente de integración con Supabase: ${name} (ver tasks.md T031).`);
}
